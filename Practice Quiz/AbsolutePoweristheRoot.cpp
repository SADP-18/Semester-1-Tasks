#include <iostream>
#include <cmath>
using namespace std;


int main() {
    float input;
    float input2; 
    
    cin >> input >> input2; 
     
   
    cout << pow(input, input2) << " " << sqrt(input) << " " << abs(input-input2);
  
    

    return 0;
}
